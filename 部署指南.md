# 水轮发电机组值班员题库 - 云端部署指南

部署后电脑关机也能在手机上使用，随时随地刷题。

---

## 方案一：Gitee Pages（国内推荐，免费，访问快）

### 准备工作
1. 注册 Gitee 账号：https://gitee.com （手机号即可注册）
2. 登录后，点击右上角 "+" → "新建仓库"

### 第一步：创建仓库
- 仓库名称：`quiz` （或任意名称）
- 仓库介绍：水轮发电机组值班员题库
- 开源：选择 "开源"
- 初始化：勾选 "使用 Readme 初始化这个仓库"（可选）
- 点击 "创建"

### 第二步：上传文件
1. 进入刚创建的仓库页面
2. 点击 "上传文件" 按钮
3. 打开本文件夹（deploy文件夹），将以下内容全部拖拽到上传区域：
   - `index.html`
   - `quiz_data_final.js`
   - `final_question_images` 文件夹（整个拖进去）
   - `final_answer_images` 文件夹（整个拖进去）
4. 等待上传完成（约26MB，根据网速可能需要1-3分钟）
5. 下方填写提交信息，如 "初始版本"
6. 点击 "提交"

### 第三步：开启 Gitee Pages
1. 在仓库页面，点击顶部菜单 "服务" → "Gitee Pages"
2. 部署分支：选择 `master` （或 `main`）
3. 部署目录：留空（默认根目录）
4. 点击 "启动"
5. 等待约1分钟，页面会显示访问地址，例如：
   ```
   https://你的用户名.gitee.io/quiz
   ```

### 第四步：手机访问
1. 手机浏览器输入上面的地址
2. 即可使用，电脑关机不影响

### 添加到手机桌面（推荐）
- iPhone：Safari打开 → 分享按钮 → 添加到主屏幕
- Android：Chrome打开 → 右上角菜单 → 添加到主屏幕

---

## 方案二：GitHub Pages（国际通用，免费）

### 准备工作
1. 注册 GitHub 账号：https://github.com
2. 登录后，点击右上角 "+" → "New repository"

### 第一步：创建仓库
- Repository name：`quiz`
- Public：选择 "Public"
- 勾选 "Add a README file"（可选）
- 点击 "Create repository"

### 第二步：上传文件
1. 在仓库页面，点击 "uploading an existing file" 链接
2. 将 deploy 文件夹中的所有文件和文件夹拖拽到上传区域
3. 等待上传完成
4. 点击 "Commit changes"

### 第三步：开启 GitHub Pages
1. 仓库页面点击 "Settings"
2. 左侧菜单找到 "Pages"
3. Source 选择 "Deploy from a branch"
4. Branch 选择 `main` / `master`，文件夹选 `/ (root)`
5. 点击 "Save"
6. 等待约1-2分钟，刷新页面会显示访问地址：
   ```
   https://你的用户名.github.io/quiz
   ```

### 注意
- GitHub Pages 在国内访问可能较慢，建议使用 Gitee 方案
- 如遇访问慢，可使用加速服务如 `ghproxy.com` 等

---

## 方案三：Vercel 拖拽部署（最简单，无需Git）

### 操作步骤
1. 访问 https://vercel.com ，用 GitHub 账号登录
2. 点击 "Add New..." → "Project"
3. 点击 "Deploy" 旁边的 "拖拽" 区域
4. 将 deploy 文件夹整体拖拽进去
5. 等待部署完成（约1分钟）
6. 获得访问地址，如 `https://quiz-xxx.vercel.app`

### 优点
- 操作最简单，无需了解Git
- 全球CDN加速，国内访问尚可
- 免费额度足够个人使用

### 缺点
- 域名是 vercel.app 后缀，国内部分网络可能不稳定
- 需要GitHub账号登录

---

## 常见问题

### Q: 上传后图片不显示？
A: 检查文件夹名称是否正确，必须是 `final_question_images` 和 `final_answer_images`，
   且与 `index.html`、`quiz_data_final.js` 在同一级目录。

### Q: Gitee Pages 启动后还是404？
A: 等待1-2分钟再刷新；确认部署分支选择正确（master/main）；
   确认仓库是公开的（开源）。

### Q: 以后更新题目怎么办？
A: 重新上传修改后的文件到仓库，Gitee Pages 需要手动在服务页面点击 "更新"；
   GitHub Pages 会自动更新。

### Q: 可以绑定自己的域名吗？
A: 可以。Gitee Pages 和 GitHub Pages 都支持自定义域名，
   在 Pages 设置页面填写域名，并在域名服务商处配置CNAME解析即可。

### Q: 数据安全吗？
A: 题库内容存储在代码平台，公开仓库任何人都能访问。
   如不希望公开，可使用私有仓库（Gitee Pages 私有仓库需要会员，
   GitHub Pages 私有仓库免费）。

---

## 文件结构说明

```
deploy/
├── index.html                  # 主页面（手机优化版）
├── quiz_data_final.js          # 题库数据（479题）
├── final_question_images/      # 题干图片（479张）
│   ├── LA5A1001_q.png
│   ├── LA5A1002_q.png
│   └── ...
└── final_answer_images/        # 答案图片（479张）
    ├── LA5A1001_a.png
    ├── LA5A1002_a.png
    └── ...
```

总大小约 26MB，完全在免费托管平台的限制范围内。
